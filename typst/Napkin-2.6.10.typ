#import "lib/napkin.typ": *
#import "lib/napkin-users.typ": *

#black_note[Exercise 2.6.10][
  Exhibit an infinite collection of open sets in $bb(R)$ whose intersection is the set ${0}$.
  This implies that infinite intersections of open sets are not necessarily open.
]

#solution(users.ghost)[
  $
    inter.big bb(R)_epsilon (0)
  $

  무한 교집합을 해버리면 $delta$이 $0$이 될 수 있음
]
