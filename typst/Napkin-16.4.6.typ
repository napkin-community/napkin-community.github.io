#import "lib/napkin.typ": *
#import "lib/napkin-users.typ": *

#black_note[Question 16.4.6][
  Why is $Z(G)$ normal in $G$?
]

#solution(users.ghost)[
  $ g Z(G) g^(-1) = g g^(-1) Z(G) = Z(G) $

  #h(1fr) #sym.qed
]
