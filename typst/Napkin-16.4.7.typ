#import "lib/napkin.typ": *
#import "lib/napkin-users.typ": *

#black_note[Question 16.4.7][
  What are the conjugacy classes of elements in the center?
]

#solution(users.ghost)[
  $ { {x} | x in Z(G) } = Z(G) $
]
