#import "lib/napkin.typ": *
#import "lib/napkin-users.typ": *

#black_note[Question 16.4.4][
  Show that the number of conjugacy classes of $S_n$ equals the number of _partitions_ of $n$.
]

#solution(users.ghost)[
  partition of $n$:
  $ a_1 + a_2 + dots.c + a_k = n $
]
